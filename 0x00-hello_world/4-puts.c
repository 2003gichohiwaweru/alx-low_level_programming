#include <stdio.h>
/**
 * Main -Aprogram that prints a new line with put function
 * Return:0(Success)
 */
int main(void)
{
puts("/"Programming is like building a a multilingual puzzle");
return(0);
}
